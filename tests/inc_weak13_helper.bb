; Hilfsdatei fuer neg_weak13_include_lines.bb — kein test_/neg_-Praefix, damit
; der Runner sie nicht als eigenen Test einsammelt. Der Fehler steht bewusst in
; Zeile 6, damit eine falsche Zuordnung sofort auffaellt.
Function Verdoppelt%(n%)
  Return n * 2
End Function
